# 使用国内权威更新源并保留 GitHub 发布镜像

LingoStack 的应用内自动更新只使用腾讯云 COS 私有源桶经已备案中国内地 CDN 域名 `lsupdates.yugasoft.cn` 暴露的静态索引与签名产物，以优先保障中国大陆网络可达性并避免多源缓存形成版本分叉；CDN 使用回源鉴权，公开更新 URL 不使用会过期的查询签名。GitHub Releases 仍保留公开发布记录和手动下载镜像，但不作为自动更新 endpoint。该选择牺牲了国内源故障时的自动切换，换取单一、可审计的最新版本判定，故失败时改为提示稍后重试或手动下载。
